---
title: 常见问题
lang: zh-CN
---

# SynoDocker 常见问题

SynoDocker 是一款 iPhone App，用于登录群晖 DSM，查看并管理 Container Manager 中的容器。

如果你的问题没有出现在下面，请在 App 中通过「联系我们」给我们发信。

## 开始使用

### SynoDocker 能做什么？

在同一局域网或可访问的网络中，登录群晖后可以：

- 查看全部容器的运行状态、映像、CPU、内存和网络概况
- 搜索、筛选运行中或已停止的容器
- 进入容器详情，查看运行统计、基本信息、网络、挂载、环境变量和日志
- 启动、停止、重启或强制停止容器
- 长按复制日志、环境变量、挂载路径、命令、网关、IP、Mac 和端口映射

查看容器详情需要订阅或一次性买断。容器列表可在登录后直接浏览。

### 需要什么环境？

- iPhone，iOS 17 或更高版本
- 已安装并启用 **Container Manager**（或旧版 Docker 套件）的群晖 NAS
- 手机能访问该 NAS 的 DSM 地址（同一 Wi-Fi、QuickConnect、或已配置 VPN / 远程访问）
- 一个有权限使用 Container Manager 的 DSM 账号

### 如何登录？

1. 打开 App，输入群晖地址：局域网 IP、DDNS 域名，或 **QuickConnect ID**
2. 若填写 IP / 域名，选择 HTTP 或 HTTPS，并确认端口（常见为 HTTP `5000`、HTTPS `5001`）
3. 若填写 QuickConnect ID，无需再填端口；App 会自动解析可达地址
4. 输入 **DSM 用户名和密码**（不是 QuickConnect 账号）
5. 如已开启两步验证，再输入一次性验证码（可点输入框旁的「粘贴」）
6. 可选开启「记住登录状态」，下次打开 App 时无需重新输入

地址栏只需填写主机名、IP 或 QuickConnect ID，不必带 `http://`。若误填了协议或路径，App 会自动去掉。

### 支持两步验证（2FA）吗？

支持。若账号开启了 OTP，登录时会提示输入验证码。验证码错误、过期或未填写时，请重新获取后再试。

### 可以使用自签名证书吗？

可以。很多群晖在局域网使用自签 HTTPS 证书，SynoDocker 可以连接这类地址。请尽量只在可信网络中使用 HTTP 或自签证书。

## 容器与详情

### 为什么列表能看，点进去却要订阅？

容器列表用于快速确认哪些容器在运行。查看详情、日志、配置和启停操作属于高级功能，需要月度订阅、年度订阅或终身买断。

### 详情页里能看到哪些信息？

- **运行统计**：CPU、内存、网络
- **基本信息**：容器 ID、映像、项目、CPU 优先顺序、内存限制、自动重启、网页门户
- **网络与策略**：网络模式、网关、IP、Mac、重启策略、端口映射
- **挂载与命令**：真实 volume 路径、启动命令
- **环境变量**：逐条 KEY / VALUE
- **最近日志**：在固定高度区域内滚动查看，并去掉 ANSI 颜色码

网关、IP、Mac、端口映射、挂载、命令可长按复制；环境变量和日志同样支持长按复制，复制成功后会有提示。

### 启动、停止、重启失败怎么办？

请确认：

- DSM 账号对该容器有操作权限
- 容器当前状态允许该操作（例如已停止的容器无法再停止）
- NAS 上 Container Manager 运行正常
- 登录未过期；若提示登录失效，请退出后重新登录

「强制停止」用于容器无响应时，请谨慎使用。

### 列表多久刷新一次？

可在「设置」中打开自动刷新，并选择 3 / 5 / 10 / 30 秒，或自定义 1–3600 秒。列表和详情页都会按该间隔更新。也可随时下拉刷新，或使用右上角菜单中的「刷新」。

## 订阅与购买

### 有哪些购买方式？

- **月度订阅**：按月自动续订
- **年度订阅**：按年自动续订
- **终身买断**：一次购买，永久使用

价格以 App Store 显示为准。订阅可随时在 Apple ID 的订阅管理中取消。

### 如何开通、恢复或取消？

- 在容器列表右上角菜单选择「查看订阅」，或到「设置」中的订阅入口
- 购买后，同一 Apple ID 换机可点「恢复购买」
- 取消自动续订：在订阅页选择「管理订阅」，或到 iPhone 的「设置 → Apple ID → 订阅」

付款由 Apple 从你的 Apple ID 扣除。订阅会在到期前 24 小时内自动续订，除非你已关闭续订。

### 为什么月度还显示有效，年度却是「待生效」？

月度和年度属于同一订阅组。从月度切换到年度时，当前月度周期通常会用完，下个计费周期才开始年度方案。此时：

- 月度：仍然有效，到期后不再按月续费
- 年度：待生效，将在当前周期结束后开始

反之从年度改到月度时也是同样逻辑。

### 买断后还要订阅吗？

不需要。终身买断会覆盖订阅权益。若之前有订阅，可在 Apple 订阅管理中自行取消，以免重复扣费。

## 无法连接或登录失败

### 提示「请输入有效的群晖地址」或连不上？

- 确认手机和 NAS 在同一网络，或 VPN / QuickConnect 等远程方式确实能访问 DSM
- 只填 IP 或域名，不要带多余路径
- 端口和 HTTP / HTTPS 与 DSM 登录页一致
- 尝试在手机 Safari 中打开同样的地址，确认 NAS 可访问
- 检查群晖防火墙、自动封锁和路由器是否拦截了该端口

### 提示「账号或密码错误」？

请使用 DSM 用户名和密码，而不是 QuickConnect ID 或 Apple ID。注意大小写，并确认账号未被禁用。

### 提示「Docker / Container Manager 不可用」？

请在套件中心安装并启动 Container Manager（部分旧系统为 Docker）。安装完成后重新登录 App。若账号没有该套件权限，也会无法读取容器。

### 提示「登录失败次数过多」？

DSM 可能暂时封锁了该账号或 IP。请稍后再试，并在 DSM 的「安全性」中查看自动封锁设置。

### 提示「登录已失效」？

会话过期或 NAS 重启后可能出现。请退出登录后重新登录。若开启了「记住登录状态」，通常只需在失效时再登录一次。

### 能用 QuickConnect ID 登录吗？

可以。在地址栏填写 QuickConnect ID（例如 `YourNAS`），或 `YourNAS.quickconnect.to`。账号和密码仍是 **DSM 用户**，不是 QuickConnect 账号。

App 会优先尝试局域网和直连地址，必要时再走 QuickConnect 中转。家里同一 Wi-Fi 通常最稳；外出时若中转失败，请改用已做端口转发的域名，或通过 VPN 连接局域网 IP。

## 隐私与数据

### 账号密码存在哪里？

登录请求直接发往你填写的群晖地址，不经过 SynoDocker 的服务器。若开启「记住登录状态」，会话信息会保存在本机钥匙串中，仅用于下次自动登录。退出登录会清除本地保存的会话。

### 你们会收集容器数据吗？

App 在本机与你的 NAS 之间通信。容器列表、日志、环境变量等不会上传到我们的服务器。购买与恢复购买由 Apple App Store 处理。

## 仍需要帮助？

请打开 App，在容器列表右上角菜单进入「关于」，再点「联系我们」。系统邮件会自动带上 App 名称和版本，请尽量补充：

- 群晖 DSM 版本、是否已安装 Container Manager
- 使用的是 HTTP 还是 HTTPS，以及是否在局域网
- 出错时的提示原文（请勿发送密码或验证码）
